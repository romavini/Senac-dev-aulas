import Aula from "../esm/index";

const main = async () => {
  console.log("BOT starting...");
  const JO = Aula({
    user: { email: "aulatest2@aula.bg", password: "qweasd123" }
  });
  await JO.connect();
  console.log("Connected");
  const changelog = await JO.userChangelogGet({});
  console.log(changelog);
};

main();
